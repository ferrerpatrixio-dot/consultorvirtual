# Handoff de sesión — 2026-08-05

**Para:** continuar en una sesión nueva de Claude Code sin perder contexto.
**Carpeta de trabajo correcta:** `C:\Users\ferre\Proyectos\CONSULTORAVIRTUAL\` (NO `PROCESOS BPMN`
— esa carpeta es un residuo de una migración incompleta del 2026-07-27, ver sección "Nota sobre
PROCESOS BPMN" al final).
**Último commit en `master`:** `ec5b635` (verificar con `git log -1` que sigue siendo el más
reciente al retomar).

---

## Prompt para gatillar en la próxima sesión (copiar y pegar tal cual)

```
Retomo el proyecto Mapea (generador-bpmn) en CONSULTORAVIRTUAL. Lee primero
docs/HANDOFF-2026-08-05.md completo para tener el contexto de la sesión anterior, y después
docs/BRECHA-MAPEA-VS-SPEC-F02.md. Ya está confirmado: Mapea va a implementar la spec F02 de
sistemaaiprocess. Arranca encargando a DEV el primer incremento que recomendó ARQUITECTO IT
(motor de reglas de completitud determinístico, sizing M) — no vuelvas a discutir la decisión,
ya está tomada. Verificá el trabajo de DEV vos mismo (tsc/eslint/build + revisión de código)
antes de subir, igual que se hizo toda la sesión anterior — el push dispara deploy automático
a producción en mapea.aiprocess.cl.
```

---

## Qué se hizo hoy (resumen ejecutivo)

1. **Producto renombrado a "Mapea"** y desplegado en producción real: `mapea.aiprocess.cl`
   (Vercel, dominio propio, DNS en Cloudflare). Login con Google funcionando, después de
   resolver tres bugs de configuración (AUTH_URL, AUTH_SECRET, AUTH_GOOGLE_SECRET).
2. **Fase 5 completa:** suscripción Mercado Pago validada de punta a punta en sandbox
   (`"status": "authorized"` real), webhook con verificación de firma HMAC implementada y
   configurada en producción.
3. **Free Trial de 3 días** implementado (1 diagrama gratis, gating a nivel de servidor).
4. **SECURITY audit:** encontró y se corrigió un hallazgo crítico (paywall evadible vía Server
   Actions).
5. **QA:** 0 bugs con ejecución real (no solo lectura de código).
6. **Rediseño del editor de pasos:** popup en vez de formulario inline, numeración, reordenar,
   resaltado del nodo en el diagrama.
7. **Límite de 4.000 caracteres** en el prompt de generación, con contador visual.
8. **DESCUBRIMIENTO GRANDE, decisión pendiente de ejecutar:** existe una especificación mucho
   más rigurosa (`sistemaaiprocess/sdd/features/F02/spec.md`, nunca implementada) para lo mismo
   que hace Mapea, con validación de completitud, coherencia SIPOC, tope de tamaño con
   descomposición, 3 niveles de corrección versionados, 4 formatos de exportación, y
   restricciones de seguridad. **Patricio confirmó: Mapea va a implementar esa spec.** El
   análisis de brecha ya está hecho — ver sección siguiente.
9. **Reorganización de archivos:** carpeta de clientes/plantillas ordenada (`docs/propuestas/`),
   prototipo histórico archivado en `generador-bpmn/docs/prototipo-historico/`.

---

## Próximo paso concreto (lo que sigue, sin re-litigar nada de lo ya decidido)

**Leer primero:** `docs/BRECHA-MAPEA-VS-SPEC-F02.md` — tiene el análisis completo de
ARQUITECTO IT: qué existe hoy en `generador-bpmn` vs. qué exige la spec F02, priorización, y
sizing.

**Primer incremento recomendado por ARQUITECTO IT (sizing M):** motor de reglas de completitud
determinístico — subset de 8 reglas de la spec (A1, A2, A3, M1, M2, M3, E2, E3 — sin E1/E1b,
que requieren un campo de entradas/salidas que no existe hoy en el modelo `Paso`) + lista
revisable antes de persistir + bloqueo de exportación con pendientes sin resolver + escapado
XML/verificación XXE (aplica ya, aunque no haya entrada por archivo, porque un usuario puede
teclear `<script>` en el texto de un paso).

**Riesgo técnico ya identificado, no ignorar:** el `[SUPUESTO]` de la spec F02 de que
`bpmn-auto-layout` soporta carriles visuales es **falso** — ya se sabía desde Fase 3 (genera el
dato semántico correcto, no dibuja el recuadro de `Lane`). No bloquea el incremento 1, pero
hay que decidirlo antes de construir exportación visual (SVG/PDF/HTML).

**Acción concreta a tomar:** encargar a DEV la implementación del incremento 1 (motor de
completitud), siguiendo el mismo patrón de esta sesión: DEV construye, PM (yo) verifica
independientemente (`tsc`/`eslint`/`build` + revisión de código), commit, push (dispara deploy
automático a `mapea.aiprocess.cl` porque el repo está conectado a Vercel).

---

## Estado de credenciales / configuración (para no repetir trabajo)

Todo esto ya está resuelto y guardado en `generador-bpmn/.env.local` (fuera de git):
- `DATABASE_URL`, `AUTH_SECRET`, `AUTH_URL` (=`https://mapea.aiprocess.cl`), `AUTH_GOOGLE_ID`/
  `AUTH_GOOGLE_SECRET` (reales, funcionando).
- `ANTHROPIC_API_KEY` (rotada hoy, la original dejó de funcionar en producción).
- `MERCADOPAGO_PUBLIC_KEY`/`ACCESS_TOKEN` (de prueba, `TEST-...`, ligadas a la app real
  `sistemaBPMNBuilder` — son las que usa la app en producción hoy).
- `MERCADOPAGO_PREAPPROVAL_PLAN_ID` (plan de prueba activo).
- `MERCADOPAGO_WEBHOOK_SECRET` (configurado y funcionando).
- `MERCADOPAGO_TEST_VENDEDOR_*` / `MERCADOPAGO_TEST_COMPRADOR_*` / `MERCADOPAGO_TEST_NESTED_*`:
  credenciales de la cuenta de prueba anidada (necesaria específicamente para probar
  `/preapproval` en sandbox — ver `docs/SPIKE-MERCADO-PAGO-BPMN-DESDE-PROMPT.md` sección 6-7 si
  hace falta repetir esa prueba).

**Todas las variables también están cargadas en Vercel** (producción). Si Vercel pide
reconfigurarlas: cuidado con el toggle "Sensitive" — no vuelve a mostrar el valor guardado una
vez creado, hay que sobreescribir si hay dudas, no confiar en que "ya estaba bien".

**Pendiente, no urgente:** pasar a credenciales de producción de Mercado Pago (`APP_USR-...`,
que Patricio ya generó pero NO están en ningún `.env` — decisión de negocio, no técnica, de
cuándo cobrar de verdad).

---

## Documentos clave generados hoy (leer según necesidad, no todos de una)

| Doc | Para qué |
|---|---|
| `docs/BRECHA-MAPEA-VS-SPEC-F02.md` | El plan técnico real a seguir ahora |
| `docs/METODOLOGIA-JERARQUIA-MAPEA.md` | Metodología validada (4 niveles: Diagrama de Valor → Proceso → Subproceso opcional → Procedimiento) |
| `docs/SPIKE-MERCADO-PAGO-BPMN-DESDE-PROMPT.md` | Todo lo aprendido de Mercado Pago, con gotchas reales |
| `docs/referencia/MERCADO-PAGO.md` | Guía general reutilizable para cualquier proyecto futuro con pagos |
| `docs/AUDITORIA-SECURITY-FASE5-MERCADOPAGO.md` | Hallazgos de seguridad (el crítico ya corregido) |
| `docs/QA-GENERADOR-BPMN-FASE6.md` | Sign-off de QA |
| `docs/NAMING-GENERADOR-BPMN.md` | Por qué se eligió "Mapea" |
| `docs/propuestas/README.md` | Cómo funciona el sistema de clientes/propuestas de la agencia |

---

## Nota sobre `PROCESOS BPMN` (la carpeta vieja)

`C:\Users\ferre\Proyectos\PROCESOS BPMN\` es un residuo de una migración a `CONSULTORAVIRTUAL`
que quedó incompleta el 2026-07-27 (ver `PROCESOS BPMN\docs\PLAN_MIGRACION_CONSULTORAVIRTUAL.md`
y `ENTREGA_FINAL\MIGRACION_CHECKLIST.md` si hace falta el detalle histórico). Contenía una copia
vieja y desactualizada de la estructura de la empresa, más un prototipo histórico
(`ENTREGA_FINAL/`) que hoy ya está copiado en `generador-bpmn/docs/prototipo-historico/`. **No
se tocó/borró la carpeta original todavía** (por seguridad, con una sesión activa ahí) — queda
pendiente que Patricio la archive o elimine cuando quiera, ya no tiene nada que no esté también
en `CONSULTORAVIRTUAL`.

**Para la próxima sesión: iniciar Claude Code desde `C:\Users\ferre\Proyectos\CONSULTORAVIRTUAL\`,
no desde `PROCESOS BPMN`.**
